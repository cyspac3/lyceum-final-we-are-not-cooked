# Team Notes

#Frontend
use types to
collect user id, name
Flashcard id: number, and contenct stored as strings.

Backend
get flashcard id
post

The webapp should use routes for when user clicks the id each flashcard should have an id attached to it.
Then "post" should return the the content of the flashcard to the user.

Yutian:
Just built some frontend - Continue finishing the study handler and set up the router, where the users click the button and will be directed to a bunch of study cards
